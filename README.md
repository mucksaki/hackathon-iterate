# webapp-template
WebApp template.

# api keys
add your `GRADIUM_API_KEY` and `PYANNOTE_API_KEY` keys to an `.env` file in the root

## Commands

- `make back-test` - Start the backend
- `make front-test` - Start the frontend
