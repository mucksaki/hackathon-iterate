[project]
name = "hackathon-iterate"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "gradium>=0.5.4",
    "requests>=2.32.5",
]
